const side1 = 10; 
const side2 = 20; 
const side3 = 30; 

const perimeter = (side1 + side2 + side3);

const area = Math.sqrt(perimeter);

console.log('sum -> ', (side1 + side2 + side3))
console.log(area);